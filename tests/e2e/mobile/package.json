{
  "name": "lectorium-e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end (Playwright) tests for the Lectorium mobile web app.",
  "scripts": {
    "build:bundle": "cd ../../../modules/apps/mobile && LECTORIUM_E2E_BUILD=1 npm run build",
    "test:bundle": "npm run build:bundle && E2E_USE_BUNDLE=1 playwright test --project=mocked",
    "test": "playwright test --project=mocked",
    "test:offline": "playwright test --project=mocked",
    "test:live": "E2E_INCLUDE_LIVE=1 playwright test --project=stack",
    "test:all": "E2E_INCLUDE_LIVE=1 playwright test",
    "test:headed": "playwright test --project=mocked --headed",
    "test:qase": "QASE_MODE=testops playwright test --project=mocked",
    "test:qase:all": "QASE_MODE=testops E2E_INCLUDE_LIVE=1 playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "playwright-qase-reporter": "^2.2.0"
  }
}
