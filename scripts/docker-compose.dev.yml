name: shruti

services:

  dev:
    image: mcr.microsoft.com/devcontainers/base:noble
    command: sleep 365d
    ports:
      - "80:80"
      - "443:443"
    networks:
      - shruti
    volumes:
      - ../:/workspaces/shruti
    depends_on:
      - couchdb
      - postgres
      - redis
      
  # -------------------------------------------------------------------------- #
  #                                  Services                                  #
  # -------------------------------------------------------------------------- #

  api:
    build:
      context: ../modules/
      dockerfile: ./services/api/Dockerfile

  couchdb:
    build:
      context: ../modules/
      dockerfile: ./services/couchdb/service/Dockerfile

  couchdb-init:
    build:
      context: ../modules/
      dockerfile: ./services/couchdb/init/Dockerfile
    environment:
      SHRUTI_CONTENT_MANAGER_EMAIL: contentManager@shruti.dev
      SHRUTI_CONTENT_MANAGER_PASSWORD: contentManager

  postgres:
    environment:
      POSTGRES_USER: shruti
      POSTGRES_PASSWORD: shruti
      POSTGRES_DB: shruti

  airflow-webserver:
    build:
      context: ../modules/
      dockerfile: ./services/airflow/Dockerfile

  airflow-scheduler:
    build:
      context: ../modules/
      dockerfile: ./services/airflow/Dockerfile

  airflow-worker:
    build:
      context: ../modules/
      dockerfile: ./services/airflow/Dockerfile

  airflow-triggerer:
    build:
      context: ../modules/
      dockerfile: ./services/airflow/Dockerfile

  airflow-init:
    build:
      context: ../modules/
      dockerfile: ./services/airflow/Dockerfile
    environment:
      _AIRFLOW_DB_MIGRATE: 'true'
      _AIRFLOW_WWW_USER_CREATE: 'true'
      _AIRFLOW_WWW_USER_USERNAME: shruti
      _AIRFLOW_WWW_USER_PASSWORD: shruti
