---
import BaseLayout from '../../layouts/BaseLayout.astro'
import WebApp from '../../components/vue/WebApp.vue'
import { useT } from '../../i18n/ui'
const t = useT('en')
---
<BaseLayout
  lang="en"
  path="/app"
  fullscreen
  title={`${t('nav.lectures')} — ${t('hero.title')}`}
  description={t('chat.band.lead')}
>
  <WebApp client:load lang="en" />
</BaseLayout>
