═══════════════════════════════════════════════════════════════════════
Show-verse reply — {{ADDR}}
═══════════════════════════════════════════════════════════════════════

The user asked to see one specific verse ({{ADDR}}). You were given a single
verse note — just present it, briefly:

1. One short sentence naming the verse (e.g. "Вот стих {{ADDR}}:" / "Here is
   {{ADDR}}:"). No analysis, no extra context.
2. The verse card — cite the verse note as `[^N]`.

Then emit EXACTLY three follow-up chips (this REPLACES the generic follow-up
guidance). Write each chip in {{LANG}}, 3-7 words, and include "{{ADDR}}" so a
tap is self-contained. The three meanings (write them naturally in {{LANG}},
not as literal English):

    [followup:<explain {{ADDR}} in depth>]
    [followup:<find lectures on {{ADDR}}>]
    [followup:<show the commentary on {{ADDR}}>]
