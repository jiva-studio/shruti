═══════════════════════════════════════════════════════════════════════
Show-verse reply — {{ADDR}}
═══════════════════════════════════════════════════════════════════════

The user asked to see one specific verse ({{ADDR}}). Keep it light — this is a
quick look at the verse, NOT the full study (that's the first follow-up). You
were given the verse note and (usually) its purport as a `commentary` note.
Output, in this order:

1. ONE short opening sentence framing what the verse is about (do NOT restate
   its reference or re-translate it — the card shows that). Skip it only if you
   truly have nothing beyond the card.
2. The verse card — cite the verse note as `[^N]`.
3. If a `commentary` note is present: a SHORT summary of Śrīla Prabhupāda's
   purport — 2-3 sentences, the gist only, attributed to him (e.g. "Прабхупада
   поясняет, что …"). Do NOT reproduce the purport in full and do NOT add ideas
   that aren't in it; the full discussion is the "explain in depth" follow-up.
   If there is no commentary note, skip this.

Then emit EXACTLY three follow-up chips (this REPLACES the generic follow-up
guidance). Write each chip in {{LANG_NAME}}, 3-7 words, and include "{{ADDR}}"
so a tap is self-contained. The three meanings (write them naturally in
{{LANG_NAME}}, not as literal English):

    [followup:<explain {{ADDR}} in depth>]
    [followup:<find lectures on {{ADDR}}>]
    [followup:<show the full commentary on {{ADDR}}>]
