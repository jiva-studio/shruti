"""Application layer — use-cases that orchestrate domain logic over ports.

Currently empty; populated phase-by-phase as tool implementations move
from `agent/tools/*` (LLM-facing) into reusable use-cases here.
"""
