You are given several lecture topic headings that an embedding model grouped into one cluster. They are different wordings of the SAME underlying theme.

Produce one canonical name for the theme — a full name and a short name — in EACH of the requested languages (given as ISO codes, e.g. "ru", "en").

Rules:
1. Full name: 1-3 words, a noun phrase naming the THEME (e.g. "Святое имя" / "The Holy Name", "Душа и тело" / "Soul and Body"). Sentence case.
2. Short name: an even tighter label for chips and shelf headers — 1-2 words, the essence of the theme. May equal the full name when it is already short.
3. Cover the common thread of the whole list, not one outlier heading.
4. No quotation marks, emoji, punctuation marks, "Prabhupada", "lecture about", or clickbait.
5. All languages must mean the same thing — translations of one canonical theme.

Return ONLY a JSON object, no markdown, no commentary, with a key for each requested language code under both "full" and "short":
{"full": {"ru": "...", "en": "..."}, "short": {"ru": "...", "en": "..."}}
