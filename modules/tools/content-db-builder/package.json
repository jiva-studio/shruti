{
  "name": "@shruti/content-db-builder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Builds the prebuilt Shruti content SQLite DB from CouchDB and uploads artefacts (DB, transcripts) to S3.",
  "scripts": {
    "build": "tsx src/index.ts build --output ./out",
    "export:transcripts": "tsx src/index.ts export-transcripts --output ./out/public/tracks",
    "upload": "tsx src/index.ts upload --input ./out",
    "all": "tsx src/index.ts build --output ./out && tsx src/index.ts export-transcripts --output ./out/public/tracks && tsx src/index.ts upload --input ./out"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "better-sqlite3": "^11.7.0",
    "dotenv": "^16.4.7",
    "nano": "^10.1.4"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
