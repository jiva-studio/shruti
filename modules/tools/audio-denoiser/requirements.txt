pyrnnoise
pydub
soundfile
numpy
scipy
tqdm
# NOTE: the default `deepfilternet` strategy uses the standalone `deep-filter`
# binary (no torch / no Python deps) — provision it via $DEEP_FILTER_BIN, PATH,
# or next to denoise_mp3.py. See github.com/Rikorose/DeepFilterNet releases.
